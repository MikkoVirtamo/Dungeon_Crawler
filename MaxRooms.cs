using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MaxRooms : MonoBehaviour
{
    public static int maxRooms;
    // Start is called before the first frame update
    void Start()
    {
        maxRooms = 5;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
